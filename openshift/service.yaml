apiVersion: v1
kind: Service
metadata:
  name: terraform-mcp-server
  namespace: ocp-mcp-server
  labels:
    app: terraform-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: terraform-mcp-server

